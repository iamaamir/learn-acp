# 0013 — Homepage as extension grid

Learner: homepage dull; liked Zed's extensions showcase grid. Rebuilt:

- Hero: kicker pill, big promise headline, sub, mono stats, solid CTA.
- Lessons as extension-style cards: title + number/minutes top row,
  one-line desc, foot status (Start here / Requires N / Next up).
  Whole card is one link; hover highlights border + title like Zed's
  selected TOML card. 0005 ships as a dimmed non-link "soon" card.
- Field manual (Mission/Sources/Glossary) as a second small card grid.
- Home column widens to 1080px with gutter lines re-anchored;
  everything else (bands, cards, code) inherits the lesson theme.
- New test: homepage lesson links resolve to real files. Suite 23/23.
